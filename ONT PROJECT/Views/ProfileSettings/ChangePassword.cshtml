@model ONT_PROJECT.Models.ChangePasswordViewModel

@{
    ViewData["Title"] = "Change Password";
    Layout = "_CustomerLayout";
}

<!-- Toast container for 3-second success popup -->
<div class="position-fixed top-0 end-0 p-3" style="z-index: 1055;">
    <div id="successToast" class="toast align-items-center text-white bg-success border-0" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="d-flex">
            <div class="toast-body">
                @TempData["SuccessMessage"]
            </div>
            <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
    </div>
</div>

<h2 class="text-dark fw-bold mb-4"><i class="bi bi-key"></i> Change Password</h2>

<ul class="nav nav-tabs mb-4">
    <li class="nav-item">
        <a class="nav-link" asp-action="Index" asp-controller="ProfileSettings">Profile</a>
    </li>
    <li class="nav-item">
        <a class="nav-link active" asp-action="ChangePassword" asp-controller="ProfileSettings">Change Password</a>
    </li>
</ul>

<div class="card shadow-sm rounded-3" style="max-width: 500px;">
    <div class="card-body p-4">
        <form asp-action="ChangePassword" method="post">
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

            <!-- Current Password -->
            <div class="mb-3">
                <label asp-for="CurrentPassword" class="form-label"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-lock"></i></span>
                    <input asp-for="CurrentPassword" type="password" class="form-control" />
                    <button class="btn btn-outline-secondary toggle-password" type="button"><i class="bi bi-eye"></i></button>
                </div>
                <span asp-validation-for="CurrentPassword" class="text-danger"></span>
            </div>

            <!-- New Password -->
            <div class="mb-3">
                <label asp-for="NewPassword" class="form-label"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-shield-lock"></i></span>
                    <input asp-for="NewPassword" type="password" class="form-control" id="newPassword" />
                    <button class="btn btn-outline-secondary toggle-password" type="button"><i class="bi bi-eye"></i></button>
                </div>
                <span asp-validation-for="NewPassword" class="text-danger"></span>

                <ul class="mt-2 mb-0 small">
                    <li id="length" class="text-danger">At least 8 characters</li>
                    <li id="uppercase" class="text-danger">Uppercase letter</li>
                    <li id="lowercase" class="text-danger">Lowercase letter</li>
                    <li id="number" class="text-danger">Number</li>
                    <li id="special" class="text-danger">Special character (@@$!%*?&)</li>
                </ul>
            </div>

            <!-- Confirm Password -->
            <div class="mb-3">
                <label asp-for="ConfirmPassword" class="form-label"></label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-shield-check"></i></span>
                    <input asp-for="ConfirmPassword" type="password" class="form-control" />
                    <button class="btn btn-outline-secondary toggle-password" type="button"><i class="bi bi-eye"></i></button>
                </div>
                <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
            </div>

            <button type="submit" class="btn btn-warning w-100 fw-bold">
                <i class="bi bi-arrow-repeat"></i> Change Password
            </button>
        </form>
    </div>
</div>


    <!-- Bootstrap JS (required for toast) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoHkM4x3sUENB1R1Q0vKjKHj1l+38U/0R8/5E1v6k1U6k8e" crossorigin="anonymous"></script>

    <!-- jQuery & validation -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.5/jquery.validate.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate-unobtrusive/3.2.12/jquery.validate.unobtrusive.min.js"></script>

    <script>
        // Toggle password visibility
        document.querySelectorAll(".toggle-password").forEach(btn => {
            btn.addEventListener("click", function () {
                const input = this.parentElement.querySelector('input');
                const icon = this.querySelector("i");
                if (input.type === "password") {
                    input.type = "text";
                    icon.classList.replace("bi-eye", "bi-eye-slash");
                } else {
                    input.type = "password";
                    icon.classList.replace("bi-eye-slash", "bi-eye");
                }
            });
        });

        // Live password strength feedback
        const newPass = document.getElementById("newPassword");
        newPass.addEventListener("input", function() {
            const val = newPass.value;
            const check = (id, condition) => {
                const el = document.getElementById(id);
                if(condition) {
                    el.className = "text-success";
                    el.textContent = "✔ " + el.textContent.slice(2);
                } else {
                    el.className = "text-danger";
                    el.textContent = "❌ " + el.textContent.slice(2);
                }
            };

            check("length", val.length >= 8);
            check("uppercase", /[A-Z]/.test(val));
            check("lowercase", /[a-z]/.test(val));
            check("number", /\d/.test(val));
            check("special", /[@@$!%*?&]/.test(val));
        });

        // Show toast if password successfully changed
        @if (TempData["SuccessMessage"] != null)
        {
                <text>
                var toastEl = document.getElementById('successToast');
                var toast = new bootstrap.Toast(toastEl, { delay: 3000 });
                toast.show();
                </text>
        }
    </script>


<style>
    body {
        font-family: 'Varela Round', sans-serif;
        color: #566787;
        background: #f5f5f5;
        font-size: 13px;
    }

    .form-control {
        border-radius: 5px;
    }

    .btn:hover {
        opacity: 0.85;
        text-decoration: none;
        color: #fff !important;
    }
</style>
