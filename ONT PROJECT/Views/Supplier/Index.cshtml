@model IEnumerable<ONT_PROJECT.Models.Supplier>
@{
    ViewData["Title"] = "Supplier";
    Layout = "_ManagerLayout";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Supplier List - Ibayi Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">
    <style>
        .card {
            margin-bottom: 20px;
        }

        .card-title {
            font-size: 1.25rem;
        }

        .card-body {
            padding: 1.25rem;
        }
    </style>
</head>
<body>

    <div class="container my-4">
        <h2 class="mb-4">Supplier List</h2>
        <hr style="height: 5px; background-color: #011425; border: none;" />

@* 
        <div class="row">
            @foreach (var supplier in Model)
            {
                <div class="col-md-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">@supplier.Name</h5>
                            <p class="card-text">Contact No: @supplier.ContactNo</p>
                            <p class="card-text">Email: @supplier.Email</p>
                            <a href="@Url.Action("Edit", "Supplier", new { id = supplier.SupplierID })" class="btn btn-warning btn-sm">Edit</a>
                            <a href="@Url.Action("Delete", "Supplier", new { id = supplier.SupplierID })" class="btn btn-danger btn-sm" onclick="return confirm('Are you sure you want to delete this supplier?')">Delete</a>
                        </div>
                    </div>
                </div>
            }
        </div> *@
    </div>
    <a asp-action="Create" asp-controller="Supplier" class="btn btn-primary mb-3">
        <i class="bi bi-plus-circle"></i> Add Supplier
    </a>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
